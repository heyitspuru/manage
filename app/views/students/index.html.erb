<p style="color: green"><%= notice %></p>

<h1>Students</h1>

<div id="students">
  <% @students.each do |student| %>
    <%= render student %>
    
  <% end %>
</div>
</br></br>
<%= link_to "New student", new_student_path, class: 'btn default' %>
